replicaCount: 1
image:
  repository: challenge12.azurecr.io/challenge

publicUrl: "challengedevops.duckdns.org"

specs:
  ingress:
    publicUrl: challengedevops.duckdns.org
    port: 80
  service:
    name: http
    port: 80
    protocol: TCP
    type: LoadBalancer

env:
  REACT_APP_API_URL: "http://challengedevops-server.duckdns.org:80"